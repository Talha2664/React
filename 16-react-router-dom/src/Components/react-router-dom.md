
1-What is React Router DOM?
```
React Router DOM ek library hai jo React app mein pages switch karne ke liye use hoti hai—without reloading the page.
```
2-How to Install:
```
npm install react-router-dom
```
3-Add <BrowserRouter> in main.jsx and also add <App> in it also import it:
```
import {BrowserRouter} from 'react-router-dom'
  <BrowserRouter>
  <App />
  </BrowserRouter>
```
Syntax:
```
Routes => Saare routes container
Route => Single page define karta hai
<Routes>
<Route><Route/>
<Routes/>
```
Link:
```
Link is used for Page switch without reload.
```